<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @subdomain_record.name %>
</p>

<%= link_to 'Parent Scan', scan_path(@subdomain_record.scan_id) %>
<%= link_to 'Back', subdomain_records_path %>
